\section{Resolución con \texttt{lpsolve}}
\subsection{Codificación del modelo}
Se presenta la formulación en formato \texttt{.lp}, con variables binarias $x_{i,d,t}$ (y $w_{i,w}$ si se usa el modelado de fines de semana), función objetivo lineal y restricciones descritas.

\subsection{Integración de datos}
Conversión de instancias a parámetros del modelo (scripts de preprocesamiento) y ejecución por lotes con \texttt{run.sh}.